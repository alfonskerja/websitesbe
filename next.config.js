/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "tailwindui.com",
        "localhost",
        "res.cloudinary.com"
      ]
    }
  }
module.exports = nextConfig
